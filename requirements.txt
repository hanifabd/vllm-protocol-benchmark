# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# Benchmark & Protocol tools
grpcio>=1.52.0
grpcio-tools>=1.52.0
protobuf>=3.20.0
pytest>=7.0.0
aiohttp>=3.8.0

# vLLM Engine (Use flexible version constraint to allow pre-built wheels)
vllm[grpc]>=0.20.0,<0.27.0