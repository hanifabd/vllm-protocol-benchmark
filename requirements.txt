# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# vLLM engine with gRPC support
vllm[grpc]==0.21.0

# Async HTTP client for REST benchmarking
aiohttp>=3.9.0

# gRPC runtime, code generation tools, and Protobuf support
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# Math and data presentation for benchmark results
numpy>=1.26.0
tabulate>=0.9.0